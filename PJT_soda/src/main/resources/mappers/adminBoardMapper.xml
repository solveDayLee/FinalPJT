<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.soda.model.dao.BoardDao">
	<select id="selectAll" resultType="Board">
		SELECT *
		FROM board
		ORDER BY
		board_no DESC
	</select>

	<select id="selectByNo" resultType="Board" parameterType="int">
		SELECT
		b.*,
		u.user_no as "user.userNo",
		u.user_id as "user.userId"
		FROM board b
		LEFT JOIN user u ON b.user_no = u.user_no
		WHERE board_no = #{no}
	</select>

	<!-- <update id="update" parameterType="User"> UPDATE user SET password 
		= #{password}, name = #{name}, email = #{email}, phone_number = #{phoneNumber}, 
		birth_date = #{birthDate}, gender= #{gender}, address= #{address}, detail_address 
		= #{detailAddress} ,zonecode = #{zonecode} WHERE user_no = #{userNo} </update> 
		<delete id="delete" parameterType="int"> DELETE from user WHERE user_no = 
		#{no} </delete> -->
	<select id="selectBoardSearched" resultType="Board"
		parameterType="SearchCondition">
		SELECT *
		FROM board
		<where>
			<if test="category == 'title'">
				title LIKE CONCAT('%', #{keyword}, '%')
			</if>
			<if test="category == 'content'">
				content LIKE CONCAT('%', #{keyword}, '%')
			</if>
			<if test="category == 'board_no'">
				board_no LIKE CONCAT('%', #{keyword}, '%')
			</if>
		</where>
	</select>
	<delete id="delete" parameterType="int">
		DELETE from board
		WHERE
		board_no = #{no}
	</delete>
	<select id="allLikesCount" resultType="int">
		SELECT
		COALESCE(COUNT(l.board_no), 0) as like_count
		FROM board b
		LEFT JOIN
		likes l ON b.board_no = l.board_no
		GROUP BY b.board_no
		ORDER BY
		b.board_no DESC;
	</select>
	<select id="likesCount" resultType="int" parameterType="int">
		SELECT COUNT(l.board_no)
		FROM board b
		LEFT JOIN likes l ON b.board_no = l.board_no
		WHERE b.board_no = #{no}
		GROUP BY b.board_no;
	</select>

</mapper>