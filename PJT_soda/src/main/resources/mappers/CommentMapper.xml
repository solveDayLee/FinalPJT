<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.soda.model.dao.CommentDao">

	<!-- 댓글 입력 -->
	<insert id="insert" parameterType="Comment">
		INSERT INTO comment (board_no, p_comment_no, comment_no, user_id, comment,
		reg_date)
		VALUES (#{boardNo}, #{pCommentNo}, #{commentNo}, #{userId}, #{comment},
		NOW());


	</insert>

	<select id="getCommentByNo" parameterType="int"
		resultType="Comment">
		select *
		from comment
		where comment_no = #{commentNo};

	</select>

	<delete id="delete" parameterType="int">
		delete from comment where comment_no = #{commentNo}
	</delete>

	<update id="update" parameterType="Comment">
		update comment
		set comment = #{comment}
		where comment_no = #{commentNo}

	</update>











</mapper>